/**
 * Placeholder. The coordinator shell and the Tablero arrive in M3, once auth and the RLS
 * policies are in place — the home screen reads real data or it does not exist.
 */
export default function Inicio() {
  return (
    <main className="mx-auto max-w-xl px-6 py-16">
      <h1 className="text-2xl font-semibold">Convite</h1>
      <p className="mt-3 text-stone-600">
        Coordinación de ayuda en la cuenca del Atrato. La pantalla de coordinación está en
        construcción.
      </p>
    </main>
  )
}
